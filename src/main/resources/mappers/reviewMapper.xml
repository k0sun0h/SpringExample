<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.sunset.spring.ex.mybatis.repository.ReviewRepository">

    <select id="selectReview"
    resultType="com.sunset.spring.ex.mybatis.domain.Review"
    parameterType="int">
		SELECT 
			`id`
			, `storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
			, `createdAt`
			, `updatedAt`
		FROM 
			`new_review`
		WHERE
			`id` = #{id}
	</select>
	
	<insert id="insertReview" parameterType="map">
		INSERT INTO 
			`new_review`
		(
			`storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
		)
		VALUE
		(
			#{storeId}
			, #{menu}
			, #{userName}
			, #{point}
			, #{review}
		)
			
	</insert>

	<insert id="insertReviewByObject" 
	parameterType="com.sunset.spring.ex.mybatis.domain.Review">
		INSERT INTO 
			`new_review`
		(
			`storeId`
			, `menu`
			, `userName`
			, `point`
			, `review`
		)
		VALUE
		(
			#{storeId}
			, #{menu}
			, #{userName}
			, #{point}
			, #{review}
		)
	</insert>
	
	<update id="updateReviewById" parameterType="map">
		UPDATE `new_review`
		SET
			`review` = #{review}
			, `updatedAt` = now()
		WHERE
			`id` = #{id} 	
 	</update>

	<delete id="deleteReviewById" parameterType="int">
 		DELETE 
 		FROM `new_review`
 		WHERE
 			`id` = #{id}	
 	</delete>
</mapper>