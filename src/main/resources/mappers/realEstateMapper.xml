<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.sunset.spring.test.mybatis.repository.RealEstateRepository">
	
	<!-- 1. id 로 select 하기 -->
	<select id="selectRealEstate" parameterType="int"
	resultType="com.sunset.spring.test.mybatis.domain.RealEstate">
		SELECT 
			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM 
			`real_estate`
		WHERE 
			`id` = #{id}
	</select>
	
	<!-- 2. 월세 조건 select -->
	<select id="selectRealEstateListByRentPrice" parameterType="int"
	resultType="com.sunset.spring.test.mybatis.domain.RealEstate">
		SELECT 
			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM 
			`real_estate`
		WHERE 
			<![CDATA[`rentPrice` < #{rent}]]>
	</select>
	
	<!-- 3. 복합조건 select -->
	<select id="selectRealEstateListByAreaAndPrice" parameterType="int"
	resultType="com.sunset.spring.test.mybatis.domain.RealEstate">
		SELECT 
			`id`
			, `realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
			, `createdAt`
			, `updatedAt`
		FROM 
			`real_estate`
		WHERE 
			<![CDATA[
			`type` = '매매'
			AND `area` >= #{area}
			AND `price` <= #{price}
			]]>
	
	</select>
	
	
	<!-- 1. 객체로 insert 하기 -->
	<insert id="insertRealEstateByObject" 
	parameterType="com.sunset.spring.test.mybatis.domain.RealEstate">
		INSERT INTO 
			`real_estate`
		(
			`realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
		)
		VALUE
		(
			#{realtorId}
			, #{address}
			, #{area}
			, #{type}
			, #{price}
		)
	</insert>
	
	<!-- 2. Parameter 로 insert 하기 -->
	<insert id="insertRealEstate" parameterType="map">
		INSERT INTO 
			`real_estate`
		(
			`realtorId`
			, `address`
			, `area`
			, `type`
			, `price`
			, `rentPrice`
		)
		VALUE
		(
			#{realtorId}
			, #{address}
			, #{area}
			, #{type}
			, #{price}
			, #{rentPrice}
		)
	</insert>
	
	<update id="updateRealEstateById" parameterType="map">
		UPDATE `real_estate`
		SET
			`type` = #{type}
			, `price` = #{price}
			, `updatedAt` = now()
		WHERE
			`id` = #{id} 	
 	</update>
 	
 	<delete id="deleteRealEstateById" parameterType="int">
 		DELETE 
 		FROM `real_estate`
 		WHERE
 			`id` = #{id}	
 	</delete>
</mapper>